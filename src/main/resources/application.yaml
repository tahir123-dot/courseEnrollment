spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/courses
    username: write you own username
    password: your password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

camunda:
  bpm:
    database:
      type: postgres
    admin-user:
      id: userid
      password: userpassword
